# After run devcontainer

```bash
git config --global user.name "John Doe"
git config --global user.email johndoe@example.com
git config --global init.defaultBranch "main"
git config --global pull.rebase false
```
